streamlit>=1.32
plotly>=5.18
pandas>=2.0
openpyxl>=3.1
requests>=2.31
Pillow>=10.0
reportlab>=4.0
# sqlite3 is included in Python standard library - no need to install
# libsql: persistencia via Turso (embedded replica). Sem TURSO_* configurado, o app usa SQLite local normalmente.
libsql>=0.1.11
